name: Rebase Upstream
on:
  schedule:
    - cron: "0 4/16 * * *"
  workflow_dispatch:   

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
        with:
          fetch-depth: 50  # greater than the number of fork commits (see FORK.md)
      - uses: imba-tjd/rebase-upstream-action@master
        with:
          branch: main

